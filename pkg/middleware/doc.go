// Package middleware provides middleware types for wrapping language and
// image models. Middleware can intercept and modify requests and responses,
// enabling cross-cutting concerns like logging, telemetry, retries, and
// custom transformations.
//
// This is the Go equivalent of the AI SDK's wrapLanguageModel and
// wrapImageModel middleware system.
package middleware
